---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/state.ts
  - src/lib/phase.ts
  - src/lib/roadmap.ts
  - src/lib/verify.ts
  - src/lib/template.ts
  - src/lib/milestone.ts
  - src/lib/commands.ts
  - src/lib/init.ts
  - src/index.ts
autonomous: true
requirements: [FOUND-02, FOUND-05, FOUND-06, FOUND-07]

must_haves:
  truths:
    - "All 11 GSD lib modules are ported to TypeScript"
    - "CLI router dispatches all commands correctly"
    - "esbuild produces a working dist/gsdr-tools.cjs bundle"
    - "State management reads and writes STATE.md with YAML frontmatter"
    - "Commit function produces atomic git commits with specific file staging"
    - "gsdr-tools.cjs executes successfully via node"
  artifacts:
    - path: "src/lib/state.ts"
      provides: "STATE.md operations and progression engine"
      exports: ["cmdStateLoad", "cmdStateJson", "cmdStateUpdate", "cmdStatePatch", "cmdStateAdvancePlan"]
    - path: "src/lib/phase.ts"
      provides: "Phase directory operations"
      exports: ["cmdFindPhase", "cmdPhaseAdd", "cmdPhaseComplete", "cmdPhasesList"]
    - path: "src/lib/roadmap.ts"
      provides: "ROADMAP.md parsing and updates"
      exports: ["cmdRoadmapGetPhase", "cmdRoadmapAnalyze", "cmdRoadmapUpdatePlanProgress"]
    - path: "src/lib/verify.ts"
      provides: "Plan structure and artifact verification"
      exports: ["cmdVerifySummary", "cmdVerifyPlanStructure", "cmdVerifyArtifacts"]
    - path: "src/lib/commands.ts"
      provides: "Utility commands (slug, timestamp, todos, scaffold, history)"
      exports: ["cmdCommit", "cmdGenerateSlug", "cmdHistoryDigest"]
    - path: "src/lib/init.ts"
      provides: "Compound init commands for workflows"
      exports: ["cmdInitExecutePhase", "cmdInitPlanPhase", "cmdInitNewProject"]
    - path: "src/index.ts"
      provides: "CLI entry point routing all commands"
      min_lines: 200
    - path: "dist/gsdr-tools.cjs"
      provides: "Bundled CJS output for runtime use"
      min_lines: 1000
  key_links:
    - from: "src/lib/state.ts"
      to: "src/lib/frontmatter.ts"
      via: "import { extractFrontmatter, reconstructFrontmatter }"
      pattern: "import.*from.*frontmatter"
    - from: "src/lib/state.ts"
      to: "src/lib/core.ts"
      via: "import { loadConfig, output, error }"
      pattern: "import.*from.*core"
    - from: "src/index.ts"
      to: "src/lib/*.ts"
      via: "imports all lib modules and routes CLI commands"
      pattern: "import.*from.*lib/"
    - from: "dist/gsdr-tools.cjs"
      to: "src/index.ts"
      via: "esbuild bundle from entry point"
      pattern: "entryPoints.*index"
---

<objective>
Port all remaining GSD lib modules to TypeScript, create the CLI router, and produce a working bundled CJS output.

Purpose: Complete the TypeScript tooling port so that dist/gsdr-tools.cjs is a fully functional drop-in replacement for the original gsd-tools.cjs.
Output: All 11 lib modules ported, CLI router working, dist/gsdr-tools.cjs built and executable.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - executor needs these contracts -->

From src/types.ts:
- GsdrConfig: mode, granularity, parallelization, commit_docs, model_profile, workflow, git
- StateFrontmatter: gsd_state_version, milestone, status, progress
- PhaseInfo: directory, phase_number, phase_name, plans, summaries
- InitResult: commit_docs, phase_found, phase_dir, plans, summaries

From src/lib/core.ts:
- output(result: unknown, raw: boolean, rawValue?: string): void
- error(message: string): never
- toPosixPath(p: string): string
- loadConfig(cwd: string): GsdrConfig
- escapeRegex(str: string): string
- MODEL_PROFILES: Record<string, Record<string, string>>

From src/lib/frontmatter.ts:
- extractFrontmatter(content: string): { frontmatter: Record<string, unknown>, body: string }
- reconstructFrontmatter(frontmatter: Record<string, unknown>, body: string): string
- cmdFrontmatterGet, cmdFrontmatterSet, cmdFrontmatterMerge, cmdFrontmatterValidate

From src/lib/config.ts:
- cmdConfigEnsureSection, cmdConfigSet, cmdConfigGet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port state.ts, phase.ts, roadmap.ts, and milestone.ts</name>
  <files>src/lib/state.ts, src/lib/phase.ts, src/lib/roadmap.ts, src/lib/milestone.ts</files>
  <action>
Port four GSD CJS modules to TypeScript. Read each source file from ~/.claude/get-shit-done/bin/lib/ and port to src/lib/.

**src/lib/state.ts** (from state.cjs, ~721 lines):
- Port all state commands: cmdStateLoad, cmdStateJson, cmdStateUpdate, cmdStateGet, cmdStatePatch, cmdStateAdvancePlan, cmdStateRecordMetric, cmdStateUpdateProgress, cmdStateAddDecision, cmdStateAddBlocker, cmdStateResolveBlocker, cmdStateRecordSession, cmdStateSnapshot
- Port stateExtractField helper
- Import extractFrontmatter/reconstructFrontmatter from frontmatter.ts
- Import loadConfig, output, error, escapeRegex, getMilestoneInfo from core.ts
- Rename any "gsd" references in state output to "gsdr" where they appear in user-facing strings

**src/lib/phase.ts** (from phase.cjs, ~901 lines):
- Port all phase commands: cmdFindPhase, cmdPhaseNextDecimal, cmdPhaseAdd, cmdPhaseInsert, cmdPhaseRemove, cmdPhaseComplete, cmdPhasesList, cmdPhasePlanIndex
- These are the largest module -- take care with the phase numbering and renumbering logic
- Import from core.ts, frontmatter.ts, roadmap.ts as needed

**src/lib/roadmap.ts** (from roadmap.cjs, ~298 lines):
- Port cmdRoadmapGetPhase, cmdRoadmapAnalyze, cmdRoadmapUpdatePlanProgress
- Import from core.ts, frontmatter.ts

**src/lib/milestone.ts** (from milestone.cjs, ~241 lines):
- Port cmdMilestoneComplete, cmdRequirementsMarkComplete
- Import from core.ts, frontmatter.ts, state.ts

For ALL modules: Convert require() to import, module.exports to named exports, add TypeScript types to all function signatures, use const instead of var/let where possible. Strip any multi-runtime conditional logic.
  </action>
  <verify>
    <automated>cd C:/projects/gsdr && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>state.ts, phase.ts, roadmap.ts, milestone.ts compile without errors. All command functions are exported. STATE.md operations use typed frontmatter. Phase operations handle numbering correctly. No require() calls remain.</done>
</task>

<task type="auto">
  <name>Task 2: Port remaining modules, CLI router, and build CJS bundle</name>
  <files>src/lib/verify.ts, src/lib/template.ts, src/lib/commands.ts, src/lib/init.ts, src/index.ts, dist/gsdr-tools.cjs</files>
  <action>
Port the remaining four lib modules, create the CLI router, and produce the bundled output.

**src/lib/verify.ts** (from verify.cjs, ~820 lines):
- Port cmdVerifySummary, cmdVerifyPlanStructure, cmdVerifyPhaseCompleteness, cmdVerifyReferences, cmdVerifyCommits, cmdVerifyArtifacts, cmdVerifyKeyLinks, cmdValidateConsistency, cmdValidateHealth
- Import from core.ts, frontmatter.ts

**src/lib/template.ts** (from template.cjs, ~222 lines):
- Port cmdTemplateSelect, cmdTemplateFill
- Import from core.ts, phase.ts

**src/lib/commands.ts** (from commands.cjs, ~548 lines):
- Port cmdResolveModel, cmdCommit, cmdGenerateSlug, cmdCurrentTimestamp, cmdListTodos, cmdVerifyPathExists, cmdHistoryDigest, cmdSummaryExtract, cmdProgressRender, cmdTodoComplete, cmdScaffold, cmdWebsearch
- cmdCommit is critical for FOUND-06 (atomic commits) -- ensure it stages specific files with `git add --` then commits
- Import from core.ts, frontmatter.ts

**src/lib/init.ts** (from init.cjs, ~710 lines):
- Port all init commands: cmdInitExecutePhase, cmdInitPlanPhase, cmdInitNewProject, cmdInitNewMilestone, cmdInitQuick, cmdInitResume, cmdInitVerifyWork, cmdInitPhaseOp, cmdInitTodos, cmdInitMilestoneOp, cmdInitMapCodebase, cmdInitProgress
- These are compound commands that aggregate context for workflows
- Import from core.ts, config.ts, state.ts, phase.ts, roadmap.ts

**src/index.ts** (port from gsd-tools.cjs CLI router, ~592 lines):
- Port the main() function and switch/case CLI router
- Import all lib modules
- Keep the --cwd and --raw flag handling
- This IS the entry point that esbuild bundles

**Build step:** After all files compile, run `node esbuild.config.mjs` to produce dist/gsdr-tools.cjs. Then verify the bundle works by running `node dist/gsdr-tools.cjs --help` or a simple command.
  </action>
  <verify>
    <automated>cd C:/projects/gsdr && npx tsc --noEmit --pretty 2>&1 | tail -3 && node esbuild.config.mjs && node dist/gsdr-tools.cjs generate-slug "test slug" 2>&1</automated>
  </verify>
  <done>All 11 lib modules + CLI router compile. esbuild produces dist/gsdr-tools.cjs without errors. Running `node dist/gsdr-tools.cjs generate-slug "test"` outputs "test" (proving the bundle executes). No require() calls in src/. Commit function (commands.ts) stages specific files and creates atomic commits.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `node esbuild.config.mjs` completes without errors
3. `node dist/gsdr-tools.cjs generate-slug "hello world"` outputs "hello-world"
4. `node dist/gsdr-tools.cjs current-timestamp date` outputs a date string
5. `grep -r "require(" src/ --include="*.ts"` returns no results
6. `wc -l dist/gsdr-tools.cjs` shows >1000 lines (full bundle)
</verification>

<success_criteria>
- All 11 GSD lib modules fully ported to TypeScript with proper types
- CLI router dispatches all commands from the original gsd-tools.cjs
- esbuild produces dist/gsdr-tools.cjs that executes correctly
- Atomic commit function preserves specific file staging behavior
- State management handles STATE.md YAML frontmatter read/write cycles
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
