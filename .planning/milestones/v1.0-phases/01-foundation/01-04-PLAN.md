---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - bin/install.js
  - vitest.config.ts
  - tests/no-multi-runtime.test.ts
  - tests/build.test.ts
  - tests/plugin-structure.test.ts
  - tests/context-engineering.test.ts
  - tests/plan-format.test.ts
  - tests/git-commits.test.ts
  - tests/state.test.ts
  - tests/package.test.ts
autonomous: true
requirements: [FOUND-01, FOUND-04, FOUND-05, FOUND-06, FOUND-07, FOUND-08]

must_haves:
  truths:
    - "npm package installs and registers as Claude Code plugin"
    - "bin/install.js copies plugin files to target directory"
    - "dist/gsdr-tools.cjs executes state and commit operations correctly"
    - "Package structure includes all required directories (skills, agents, dist, templates, references)"
    - "Tests validate all 8 FOUND requirements across 8 dedicated test files"
  artifacts:
    - path: "bin/install.js"
      provides: "npm bin entry that installs GSDR plugin"
      min_lines: 30
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
    - path: "tests/no-multi-runtime.test.ts"
      provides: "FOUND-01: No multi-runtime code validation"
      contains: "OpenCode"
    - path: "tests/build.test.ts"
      provides: "FOUND-02: Build output validation"
      contains: "gsdr-tools.cjs"
    - path: "tests/plugin-structure.test.ts"
      provides: "FOUND-03: Plugin directory structure validation"
      contains: "plugin.json"
    - path: "tests/context-engineering.test.ts"
      provides: "FOUND-04: Context engineering pattern validation"
      contains: "files_to_read"
    - path: "tests/plan-format.test.ts"
      provides: "FOUND-05: XML plan format validation"
      contains: "task"
    - path: "tests/git-commits.test.ts"
      provides: "FOUND-06: Atomic git commit validation"
      contains: "commit"
    - path: "tests/state.test.ts"
      provides: "FOUND-07: STATE.md operations testing"
      contains: "STATE"
    - path: "tests/package.test.ts"
      provides: "FOUND-08: Package.json validation"
      contains: "gsdr"
  key_links:
    - from: "bin/install.js"
      to: ".claude-plugin/plugin.json"
      via: "copies plugin directory to install target"
      pattern: "claude-plugin"
    - from: "bin/install.js"
      to: "dist/gsdr-tools.cjs"
      via: "copies dist/ to install target"
      pattern: "dist"
    - from: "tests/build.test.ts"
      to: "dist/gsdr-tools.cjs"
      via: "validates bundle exists and executes"
      pattern: "gsdr-tools"
---

<objective>
Create the npm distribution install script and complete Wave 0 validation test suite covering ALL 8 FOUND requirements.

Purpose: Ensure the package can be installed via `npx gsdr@latest` and that all critical systems are validated by dedicated test files per the VALIDATION.md contract.
Output: Working install script, 8 test files covering FOUND-01 through FOUND-08, all tests passing.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
From package.json (Plan 01):
- bin.gsdr -> "bin/install.js"
- files: ["bin", ".claude-plugin", "skills", "agents", "hooks", "templates", "references", "dist"]
- scripts.build -> "node esbuild.config.mjs"
- scripts.test -> "vitest run"

From .claude-plugin/plugin.json (Plan 01):
- name: "gsdr"

From dist/gsdr-tools.cjs (Plan 02):
- CLI tool accepting commands: state, commit, generate-slug, find-phase, etc.
- Supports --cwd and --raw flags

From skills/ (Plan 03):
- 30+ SKILL.md files in skills/*/SKILL.md format
- All use /gsdr: namespace
- Skills with agent delegation have context: fork

From agents/ (Plan 03):
- 12 agent files as agents/gsdr-*.md
- Agents use path-only context references (files_to_read with @path)
- Executor agent preserves XML plan format (task/checkpoint/verify tags)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create npm install script and test framework setup</name>
  <files>bin/install.js, vitest.config.ts</files>
  <action>
**bin/install.js** -- Create the npm package entry point that installs GSDR as a Claude Code plugin:

1. Add `#!/usr/bin/env node` shebang
2. Determine source directory (path.join(__dirname, '..')) -- this is the npm package root
3. Determine target directory. Per research Open Question 1, use this strategy:
   - Primary: `~/.claude/plugins/local/gsdr/` (for plugin-dir based loading)
   - Copy plugin files: .claude-plugin/, skills/, agents/, hooks/, templates/, references/, dist/
   - Use recursive directory copy (fs.cpSync with {recursive: true} on Node 18+, or manual recursive copy for broader compat)
4. After copy, print instructions:
   - "GSDR plugin installed to {TARGET_DIR}"
   - "To use: claude --plugin-dir {TARGET_DIR}" or add to settings
   - "Then run /gsdr:new-project to begin"
5. Handle errors gracefully (permission denied, missing dirs)
6. Support `--uninstall` flag to remove the plugin directory
7. Support `--help` flag to print usage info (without performing any install)

**vitest.config.ts** -- Create test framework configuration:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['tests/**/*.test.ts'],
    testTimeout: 30000,
  },
});
```

Run `npm install -D vitest` if not already in node_modules.
  </action>
  <verify>
    <automated>cd C:/projects/gsdr && node --check bin/install.js && echo "syntax valid" && test -f vitest.config.ts && echo "vitest config exists"</automated>
  </verify>
  <done>bin/install.js exists with shebang, passes syntax check. vitest.config.ts exists with Node environment. Neither triggers side effects during verification.</done>
</task>

<task type="auto">
  <name>Task 2: Create complete Wave 0 validation test suite for all FOUND requirements</name>
  <files>tests/no-multi-runtime.test.ts, tests/build.test.ts, tests/plugin-structure.test.ts, tests/context-engineering.test.ts, tests/plan-format.test.ts, tests/git-commits.test.ts, tests/state.test.ts, tests/package.test.ts</files>
  <action>
Create ALL 8 test files required by VALIDATION.md Wave 0. Each file validates one FOUND requirement. Reference VALIDATION.md for the exact mapping.

**tests/no-multi-runtime.test.ts** (FOUND-01: Multi-runtime code stripped):
- Read dist/gsdr-tools.cjs content as string
- Test it does NOT contain "OpenCode" (case-sensitive)
- Test it does NOT contain "Gemini" (as runtime reference, not in general text)
- Test it does NOT contain "Codex" (as runtime reference)
- Test it does NOT contain "Copilot" (as runtime reference)
- Scan all src/**/*.ts files for the same forbidden strings
- Test that MODEL_PROFILES only contains Claude Code model names (gsdr-* prefixed agents, not gsd-*)

**tests/build.test.ts** (FOUND-02: TypeScript compiles, esbuild produces CJS):
- Test that dist/gsdr-tools.cjs exists and is >1000 lines
- Test that dist/gsdr-tools.cjs is valid CJS (can be required without error)
- Test that running `node dist/gsdr-tools.cjs generate-slug "test value"` outputs "test-value"
- Test that running `node dist/gsdr-tools.cjs current-timestamp date` outputs a date string matching YYYY-MM-DD pattern

**tests/plugin-structure.test.ts** (FOUND-03: Claude Code Plugin format):
- Test that .claude-plugin/plugin.json exists and contains name "gsdr"
- Test that skills/ directory exists with at least 25 SKILL.md files
- Test that agents/ directory exists with at least 10 gsdr-*.md files
- Test that hooks/hooks.json exists and is valid JSON
- Test that templates/ directory exists with at least 10 .md files
- Test that references/ directory exists with at least 5 .md files
- Test that key skills exist: plan-phase, execute-phase, new-project, research-phase
- Test that key agents exist: gsdr-executor, gsdr-planner, gsdr-phase-researcher

**tests/context-engineering.test.ts** (FOUND-04: Context engineering preserved):
- Read agents/gsdr-executor.md (or equivalent primary agent)
- Test that it contains `files_to_read` block (path-based context passing)
- Test that agent files do NOT inline large file contents (no 500+ character code blocks that should be @path references)
- Test that skills with `context: fork` properly delegate to agents
- Read 2-3 representative skills and verify they use `${CLAUDE_SKILL_DIR}` for path references, not hardcoded paths

**tests/plan-format.test.ts** (FOUND-05: XML plan format preserved):
- Read agents/gsdr-executor.md (or the executor workflow reference)
- Test it contains `<task` tags (XML task format)
- Test it contains `<verify>` tags
- Test it contains checkpoint patterns (`checkpoint:human-verify` or similar)
- Read agents/gsdr-planner.md and verify it references XML plan structure

**tests/git-commits.test.ts** (FOUND-06: Atomic git commits):
- Read src/lib/commands.ts (or dist/gsdr-tools.cjs)
- Test that cmdCommit function exists (grep for export or function definition)
- Test that the commit implementation uses `git add --` with specific files (not `git add -A` or `git add .`)
- Test that commit creates atomic commits (stages specific files, then commits)
- If possible, run a dry-run commit test in a tmpdir with a git repo

**tests/state.test.ts** (FOUND-07: State management):
- Create a temporary directory with a .planning/STATE.md file
- Test that gsdr-tools.cjs can read state from that directory (using --cwd flag)
- Test state json output parses as valid JSON
- Test state update modifies a field
- Test state get retrieves content
- Use the tmpdir for isolation, clean up after

**tests/package.test.ts** (FOUND-08: npm package distribution):
- Test that package.json exists with name "gsdr"
- Test that package.json has bin.gsdr pointing to "bin/install.js"
- Test that bin/install.js exists and has shebang
- Test that package.json files array includes required directories
- Test that package.json scripts.build exists
- Test that all directories listed in files array exist on disk

Use vitest describe/it/expect patterns. Import from 'vitest' if globals not configured. Use path.join(__dirname, '..') to reference project root. Each test file should be self-contained and runnable individually via `npx vitest run tests/{file}.test.ts -x`.
  </action>
  <verify>
    <automated>cd C:/projects/gsdr && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 8 test files exist. npx vitest run passes all tests. Tests cover every FOUND requirement: FOUND-01 (no multi-runtime), FOUND-02 (build), FOUND-03 (plugin structure), FOUND-04 (context engineering), FOUND-05 (plan format), FOUND-06 (git commits), FOUND-07 (state), FOUND-08 (package). Wave 0 from VALIDATION.md is fully satisfied.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` passes all tests
2. `node --check bin/install.js` passes syntax validation
3. `ls tests/*.test.ts | wc -l` shows 8 test files
4. Test coverage spans all FOUND requirements: FOUND-01 through FOUND-08
5. Each test file runnable individually: `npx vitest run tests/{name}.test.ts -x`
</verification>

<success_criteria>
- Install script passes syntax check and has correct structure
- All 8 test files pass via vitest (one per FOUND requirement)
- Wave 0 from VALIDATION.md fully satisfied (all 9 checkboxes: vitest.config.ts + 8 test files)
- No multi-runtime code in build output (FOUND-01 verified by test)
- Package is distributable via npm (correct bin, files, scripts)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
