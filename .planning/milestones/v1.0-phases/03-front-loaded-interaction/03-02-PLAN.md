---
phase: 03-front-loaded-interaction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/new-project/SKILL.md
  - tests/upfront-session.test.ts
autonomous: true
requirements: [AUTO-01, AUTO-02]

must_haves:
  truths:
    - "new-project skill gathers per-phase CONTEXT.md decisions after roadmap creation"
    - "System performs preliminary codebase analysis and complexity classification before asking questions"
    - "Question depth adapts by complexity tier: Simple gets 1-2 questions, Medium gets 3-4, Complex gets full discuss-phase treatment"
    - "Auto mode with --auto @document.md generates per-phase CONTEXT.md from document without interactive questions"
    - "Analysis summary is displayed to user before questioning starts"
    - "Context window limit triggers automatic .continue-here.md write and resume-work invocation"
  artifacts:
    - path: "skills/new-project/SKILL.md"
      provides: "Step 8.5 with preliminary analysis and per-phase questioning loop"
      contains: "8.5"
    - path: "tests/upfront-session.test.ts"
      provides: "Tests for AUTO-01 and AUTO-02 requirements"
  key_links:
    - from: "skills/new-project/SKILL.md"
      to: "classifyComplexity"
      via: "classify-complexity CLI command called per phase"
      pattern: "classify-complexity"
    - from: "skills/new-project/SKILL.md"
      to: "CONTEXT.md"
      via: "writes per-phase CONTEXT.md after questioning"
      pattern: "CONTEXT.md"
    - from: "skills/new-project/SKILL.md"
      to: "discuss-phase patterns"
      via: "inlined gray area identification (not spawned as sub-skill)"
      pattern: "gray.area"
    - from: "skills/new-project/SKILL.md"
      to: ".continue-here.md"
      via: "auto-writes handoff and invokes resume-work on context limit"
      pattern: "continue-here"
---

<objective>
Extend new-project skill to gather all phase context upfront after roadmap creation, with preliminary analysis informing the questions.

Purpose: AUTO-01 requires all user questions in one upfront session. AUTO-02 requires preliminary analysis before questioning. This plan adds a Step 8.5 to new-project that iterates through each phase, classifies complexity, analyzes the codebase, and gathers CONTEXT.md decisions -- all before the user walks away.
Output: Extended new-project SKILL.md with post-roadmap questioning loop.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-front-loaded-interaction/03-RESEARCH.md
</context>

<interfaces>
<!-- Key patterns to reuse from existing skills -->

From skills/new-project/SKILL.md (current structure):
```
Step 1: Setup (init)
Step 2: Brownfield Offer
Step 2a: Auto Mode Config
Step 3: Deep Questioning
Step 4: Write PROJECT.md
Step 5: Workflow Preferences
Step 5.5: Resolve Model Profile
Step 6: Research Decision
Step 7: Define Requirements
Step 8: Create Roadmap
Step 9: Done
```
Step 8.5 will be inserted between Step 8 (Create Roadmap) and Step 9 (Done).

From skills/discuss-phase/SKILL.md (patterns to inline, NOT spawn):
```
- Gray area identification based on domain type
- 4-question-then-check pattern via AskUserQuestion
- CONTEXT.md template structure with <decisions>, <specifics>, <deferred>, <code_context>
- Scope guardrail to prevent scope creep
```

From src/lib/complexity.ts:
```typescript
// CLI command: classify-complexity "$PHASE_GOAL"
// Returns JSON with prompt field to evaluate for tier classification
// Tiers: simple, medium, complex
```

From existing auto-advance in new-project Step 2a:
```json
// Config already sets auto_advance: true and _auto_chain_active: true
// Step 9 already auto-advances to discuss-phase 1 --auto
```

From skills/pause-work/SKILL.md (.continue-here.md pattern):
```
- Writes handoff to .planning/phases/XX-name/.continue-here.md
- Contains: phase, plan, task, remaining work, context needed
- Committed with: "wip: [phase-name] paused at task [X]/[Y]"
- Resumed via /gsdr:resume-work which reads .continue-here.md
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Step 8.5 to new-project skill with preliminary analysis and per-phase questioning</name>
  <files>skills/new-project/SKILL.md, tests/upfront-session.test.ts</files>
  <behavior>
    - Test 1: new-project SKILL.md contains "## 8.5" step heading
    - Test 2: new-project SKILL.md Step 8.5 references classify-complexity for per-phase classification
    - Test 3: new-project SKILL.md Step 8.5 contains Simple/Medium/Complex tier routing with different question depths
    - Test 4: new-project SKILL.md Step 8.5 contains CONTEXT.md generation and commit per phase
    - Test 5: new-project SKILL.md Step 8.5 auto mode section generates CONTEXT.md from document without interactive questions
    - Test 6: new-project SKILL.md Step 8.5 contains preliminary analysis section with codebase analysis for brownfield projects
    - Test 7: new-project SKILL.md Step 8.5 contains analysis summary display before questioning
    - Test 8: new-project SKILL.md Step 8.5 contains auto-resume with .continue-here.md on context window limit (not interactive offer)
  </behavior>
  <action>
1. Create `tests/upfront-session.test.ts` with the tests described in behavior section. Tests should read `skills/new-project/SKILL.md` as a string and assert content patterns. Test 8 specifically must verify that the skill writes `.continue-here.md` automatically and invokes `resume-work` -- NOT that it "offers" or "warns" the user.

2. In `skills/new-project/SKILL.md`, insert a new `## 8.5. Upfront Phase Context Gathering` section between Step 8 (Create Roadmap) and Step 9 (Done). The section should contain:

**Auto mode branch:**
- For each phase in the roadmap, extract relevant decisions from the provided document
- Match by requirement IDs and phase goal keywords
- Generate CONTEXT.md for each phase with matched content as locked decisions and unaddressed areas as "Claude's Discretion"
- Commit all CONTEXT.md files

**Interactive mode branch:**

**Preliminary Analysis subsection (AUTO-02):**
- Display banner: `GSDR > PHASE CONTEXT GATHERING`
- Read ROADMAP.md and extract all phases with goals and requirements
- For each phase: classify complexity using `node "${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs" classify-complexity "$PHASE_GOAL"` then evaluate the returned prompt to determine tier
- If brownfield (`has_codebase_map` or `needs_codebase_map` from init): read codebase maps or run targeted grep to cross-reference phase requirements against existing capabilities, identifying already-solved problems
- Display analysis summary table showing phase complexity tiers and brownfield findings:
  ```
  Phase Complexity Analysis:
  | Phase | Complexity | Key Areas |
  |-------|-----------|-----------|
  | 1. [Name] | Simple | [areas] |
  | 2. [Name] | Complex | [areas] |

  [If brownfield]: Found existing [description]. Skipping questions for: [already-solved items]
  ```

**Per-Phase Questioning subsection (AUTO-01):**
- For each phase (in order):
  - Display: `Phase N: [Name] ([Tier])`
  - Route by complexity tier:
    - **Simple:** 1-2 AskUserQuestion calls covering only key decisions. Generate CONTEXT.md with most areas marked as "Claude's Discretion".
    - **Medium:** Identify 2-3 gray areas using discuss-phase gray area identification pattern (inlined, NOT spawned as sub-skill to avoid nesting issue #686). Ask 3-4 questions via AskUserQuestion. Generate CONTEXT.md.
    - **Complex:** Run full gray area identification (inlined from discuss-phase pattern). Present gray areas for user to select. Discuss selected areas with 4-question-then-check loop. Generate CONTEXT.md.
  - For brownfield: skip questions about capabilities identified as already-solved during preliminary analysis
  - Write CONTEXT.md to phase directory using the standard CONTEXT.md template format (with `<decisions>`, `<specifics>`, `<deferred>`, `<code_context>` sections)
  - Commit each CONTEXT.md:
    ```bash
    node "${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs" commit \
      "docs(${PADDED}): capture phase context" \
      --files ".planning/phases/${PADDED}-${SLUG}/${PADDED}-CONTEXT.md"
    ```

**Set Auto-Advance subsection:**
- After all phases questioned, set `workflow.auto_advance = true` and `workflow._auto_chain_active = true` via config-set commands
- These are already set in auto mode (Step 2a), but interactive mode needs them set here

**Context Window Auto-Resume subsection:**
- Before each phase iteration, check approximate token usage. If session approaches 150K tokens (estimated by counting phases completed vs remaining and message history length):
  - Automatically write `.continue-here.md` to the current phase directory with full state:
    - Which phases have been completed (with their CONTEXT.md paths)
    - Which phases remain (with their complexity tiers from preliminary analysis)
    - Current position in the questioning loop
    - Any brownfield analysis results still applicable
  - Commit the handoff file:
    ```bash
    node "${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs" commit \
      "wip: upfront session paused at phase ${CURRENT_PHASE}" \
      --files ".planning/phases/${CURRENT_PHASE_DIR}/.continue-here.md"
    ```
  - Display: `Context limit approaching. Saving state and resuming automatically...`
  - Auto-invoke resume-work to pick up seamlessly in a fresh context window:
    ```
    /gsdr:resume-work
    ```
  - Do NOT present this as an interactive choice or warning. The handoff and resume must happen automatically per locked decision.

3. Update Step 9 (Done) to reflect the new flow:
- In auto mode: instead of auto-advancing to `discuss-phase 1 --auto`, auto-advance directly to `plan-phase 1 --auto` (since context is already gathered in Step 8.5)
- In interactive mode: instead of suggesting `/gsdr:discuss-phase 1`, suggest `/gsdr:plan-phase 1` (since context was gathered in Step 8.5)
- Keep `discuss-phase` available as "Also available" option for mid-project course corrections
  </action>
  <verify>
    <automated>npx vitest run tests/upfront-session.test.ts</automated>
  </verify>
  <done>new-project SKILL.md has Step 8.5 with preliminary analysis (brownfield detection, complexity classification, summary display) and per-phase questioning loop (tier-adaptive question depth, CONTEXT.md generation per phase, auto mode document extraction); context window limit auto-writes .continue-here.md and auto-invokes resume-work; Step 9 routes to plan-phase instead of discuss-phase; all tests pass</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/upfront-session.test.ts` -- all AUTO-01 and AUTO-02 tests pass
2. `grep "8.5" skills/new-project/SKILL.md` -- confirms Step 8.5 exists
3. `grep "classify-complexity" skills/new-project/SKILL.md` -- confirms per-phase complexity classification
4. `grep "Simple\|Medium\|Complex" skills/new-project/SKILL.md` -- confirms tier routing in Step 8.5
5. `grep "CONTEXT.md" skills/new-project/SKILL.md` -- confirms CONTEXT.md generation
6. `grep "continue-here" skills/new-project/SKILL.md` -- confirms auto-resume handoff
7. `grep "resume-work" skills/new-project/SKILL.md` -- confirms auto-invocation of resume-work
</verification>

<success_criteria>
- new-project SKILL.md has Step 8.5 between roadmap creation and done
- Preliminary analysis runs before questioning: codebase analysis for brownfield, complexity classification for all phases
- Analysis summary displayed to user before first question
- Per-phase questioning adapts depth by complexity tier (Simple: 1-2 questions, Medium: 3-4, Complex: full discuss-phase treatment)
- Gray area identification pattern is inlined (not spawned as sub-skill) to avoid nesting issue #686
- Each phase gets a committed CONTEXT.md
- Auto mode extracts context from document without interactive questions
- Context window limit automatically writes .continue-here.md and invokes resume-work (no interactive prompt)
- Step 9 routes to plan-phase (not discuss-phase) since context is already gathered
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-front-loaded-interaction/03-02-SUMMARY.md`
</output>
