---
phase: 04-dependency-graph-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - skills/execute-phase/SKILL.md
  - skills/plan-phase/SKILL.md
autonomous: true
requirements:
  - PARA-03
  - PARA-04
must_haves:
  truths:
    - "Within-phase wave execution still works identically to before (execute-phase single-phase mode unchanged)"
    - "execute-phase can operate in cross-phase mode, running multiple phases whose dependencies are satisfied"
    - "plan-phase can plan multiple phases concurrently when their dependencies are met"
    - "Cross-phase mode generates dependency-graph.json before scheduling"
    - "Agent count is checked before spawning any batch"
  artifacts:
    - path: "skills/execute-phase/SKILL.md"
      provides: "Cross-phase orchestration mode for execute-phase"
      contains: "execute_milestone"
    - path: "skills/plan-phase/SKILL.md"
      provides: "Multi-phase parallel planning support"
      contains: "parallel_planning"
  key_links:
    - from: "skills/execute-phase/SKILL.md"
      to: "src/lib/scheduler.ts"
      via: "CLI call to dependency-graph command"
      pattern: "dependency-graph"
    - from: "skills/plan-phase/SKILL.md"
      to: "src/lib/scheduler.ts"
      via: "CLI call to dependency-graph command for phase ordering"
      pattern: "dependency-graph"
---

<objective>
Wire the scheduler into execute-phase and plan-phase skills to enable cross-phase parallel execution and parallel planning. Keep existing single-phase mode as default; cross-phase mode is opt-in.

Purpose: The scheduler from Plan 01 provides the computation. This plan connects it to the skill orchestrators so that Claude agents actually use the dependency graph during execution.
Output: Updated execute-phase/SKILL.md with cross-phase mode, updated plan-phase/SKILL.md with multi-phase planning
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dependency-graph-engine/04-RESEARCH.md
@.planning/phases/04-dependency-graph-engine/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - scheduler.ts exports -->
From src/lib/scheduler.ts:
```typescript
export interface DependencyGraph {
  generated: string;
  phases: PhaseNode[];
  edges: DependencyEdge[];
  batches: ExecutionBatch[];
}

export interface PhaseNode {
  phase_number: string;
  phase_name: string;
  depends_on: string[];
  status: 'complete' | 'planned' | 'pending';
  plans: PlanSummary[];
}

export interface PlanSummary {
  id: string;
  wave: number;
  files_modified: string[];
  autonomous: boolean;
}

export interface ExecutionBatch {
  batch_number: number;
  phases: string[];
  max_agents: number;
  file_conflicts: FileConflict[];
}

export interface FileConflict {
  file: string;
  plans: string[];
  resolution: 'sequential';
}

export interface AgentBudget {
  phase: string;
  allocated_agents: number;
  total_plans: number;
}

// CLI command: node dist/gsdr-tools.cjs dependency-graph [--cwd path] [--raw]
// Output: writes .planning/dependency-graph.json, returns JSON summary
export function cmdDependencyGraph(cwd: string, raw: boolean): void;
```

From skills/execute-phase/SKILL.md (current structure):
- Step: load_phase_init -> discover_plans -> display_overview -> execute_waves -> complete_phase
- Wave execution: for each wave, spawn Task agents for each plan in the wave
- parallelization config controls whether wave plans run parallel or sequential

From skills/plan-phase/SKILL.md (current structure):
- Step: load_project_state -> identify_phase -> gather_phase_context -> write_phase_prompt
- Currently plans one phase at a time
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-phase orchestration mode to execute-phase skill</name>
  <files>skills/execute-phase/SKILL.md</files>
  <action>
Add a new section to execute-phase/SKILL.md for cross-phase execution mode. This is additive -- do NOT modify existing single-phase behavior.

Add after the existing `<step name="load_phase_init">` section, a new conditional path:

1. Add a new step `<step name="check_cross_phase_mode">` that checks if the skill was invoked with `--milestone` or `--all` flag:
   - If flag present: enter cross-phase mode
   - If not: proceed with existing single-phase execution (unchanged)

2. For cross-phase mode, add `<step name="execute_milestone">`:
   a. Generate dependency graph: `node dist/gsdr-tools.cjs dependency-graph --cwd "$CWD"`
   b. Read .planning/dependency-graph.json
   c. For each batch in batches[]:
      - Check file_conflicts -- if any exist, log warning and run conflicting plans sequentially
      - Compute agent budget via batch.max_agents (already capped by scheduler)
      - For each phase in batch.phases:
        - Spawn execute-phase for that single phase (reuse existing single-phase mode)
        - This preserves within-phase wave execution (PARA-03)
      - Wait for all phases in batch to complete
      - Verify all phases completed (check SUMMARYs)
      - Proceed to next batch
   d. After all batches: run completion logic

3. Add agent budget check before spawning: if total plans in batch > max_agents, use distributeAgentBudget logic from dependency-graph.json to cap agents per phase. Log: "Budget: {allocated}/{total} agents for phase {N}".

4. Key constraint: within each phase, the existing wave-based execution is PRESERVED. Cross-phase mode simply determines WHICH phases to run and WHEN, then delegates to single-phase execute-phase for each.

5. Add to the skill's description/frontmatter that it supports `--milestone` flag for cross-phase execution.

6. Do NOT change existing single-phase behavior or step ordering. The cross-phase mode is a new conditional branch that wraps the existing logic.
  </action>
  <verify>
    <automated>[ $(grep -c -E "execute_milestone|cross.phase|dependency-graph" skills/execute-phase/SKILL.md) -ge 3 ] && echo "PASS: found required keywords" || echo "FAIL: missing keywords"</automated>
  </verify>
  <done>execute-phase/SKILL.md contains cross-phase orchestration mode that generates dependency graph, processes batches respecting agent limits, and delegates to single-phase execution per phase. Existing single-phase mode is unchanged. PARA-03 is preserved because cross-phase mode delegates to single-phase execute-phase which already handles within-phase wave ordering. Also verify via: npx vitest run tests/dependency-graph.test.ts -t "PARA-03"</done>
</task>

<task type="auto">
  <name>Task 2: Add multi-phase parallel planning to plan-phase skill</name>
  <files>skills/plan-phase/SKILL.md</files>
  <action>
Add multi-phase parallel planning support to plan-phase/SKILL.md. This enables planning multiple phases concurrently when their dependencies are satisfied (PARA-04).

1. Add a new step `<step name="parallel_planning">` that activates when plan-phase is invoked with `--all` or `--milestone` flag:
   a. Generate dependency graph: `node dist/gsdr-tools.cjs dependency-graph --cwd "$CWD"`
   b. Read .planning/dependency-graph.json
   c. Identify phases that need planning (status !== 'complete', have no PLAN.md files yet)
   d. Group plannable phases into batches using the dependency graph batches
   e. For each batch:
      - For each phase that needs planning in this batch:
        - Spawn plan-phase for that single phase (reuse existing single-phase mode)
      - Wait for all planning agents to complete
      - Regenerate dependency graph (plans now exist with files_modified)
      - Proceed to next batch

2. Add to the step ordering: check for --all/--milestone flag BEFORE identify_phase step. If flag present, route to parallel_planning. If not, proceed with existing single-phase planning.

3. Agent budget for planning: planning agents are lighter than execution agents. Cap at max_concurrent_agents from config (default 5). Each planning agent counts as 1 agent regardless of phase size.

4. Key constraint: do NOT modify existing single-phase planning behavior. This is a new conditional branch.

5. After all phases are planned, regenerate dependency-graph.json one final time so it reflects all newly-created plans with their files_modified declarations.

6. Add to skill frontmatter/description that it supports `--all`/`--milestone` flags.
  </action>
  <verify>
    <automated>[ $(grep -c -E "parallel_planning|--milestone|--all|dependency-graph" skills/plan-phase/SKILL.md) -ge 3 ] && echo "PASS: found required keywords" || echo "FAIL: missing keywords"</automated>
  </verify>
  <done>plan-phase/SKILL.md contains multi-phase parallel planning mode that uses dependency graph to plan multiple phases concurrently. Existing single-phase mode is unchanged.</done>
</task>

</tasks>

<verification>
1. grep for "execute_milestone" in skills/execute-phase/SKILL.md confirms cross-phase mode exists
2. grep for "parallel_planning" in skills/plan-phase/SKILL.md confirms multi-phase planning exists
3. Both skills still contain all original single-phase steps (no regressions)
4. Both skills reference dependency-graph CLI command for graph generation
5. Both skills document the --milestone/--all flag in their description
6. npx vitest run tests/dependency-graph.test.ts -t "PARA-03" confirms wave preservation test passes (test created in Plan 01)
</verification>

<success_criteria>
- execute-phase supports --milestone flag for cross-phase batch execution
- plan-phase supports --all/--milestone flag for multi-phase parallel planning
- Cross-phase mode generates dependency-graph.json before scheduling
- Within-phase wave execution is preserved (PARA-03) -- single-phase mode unchanged
- Agent count is checked against budget before spawning any batch
- File conflicts from dependency graph are respected (conflicting plans run sequentially)
</success_criteria>

<output>
After completion, create `.planning/phases/04-dependency-graph-engine/04-02-SUMMARY.md`
</output>
