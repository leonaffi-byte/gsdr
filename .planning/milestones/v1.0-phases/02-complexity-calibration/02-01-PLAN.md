---
phase: 02-complexity-calibration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/complexity.ts
  - src/types.ts
  - src/index.ts
  - tests/complexity.test.ts
autonomous: true
requirements:
  - CMPLX-01
  - CMPLX-04

must_haves:
  truths:
    - "classifyComplexity() returns a ComplexityResult with tier, confidence, rationale, and signals for any task description"
    - "COMPLEXITY_TIERS config maps each tier to research/plan-count/checker/verifier settings"
    - "CLI command classify-complexity outputs JSON with tier and rationale"
    - "When --complexity flag provides an override, the override tier is used instead of auto-detect"
    - "18+ of 20 labeled test cases classify to the expected tier"
  artifacts:
    - path: "src/lib/complexity.ts"
      provides: "Complexity classifier module with types, config, prompt template, classify function"
      exports: ["ComplexityResult", "ComplexityConfig", "COMPLEXITY_TIERS", "CLASSIFIER_PROMPT", "classifyComplexity", "getComplexityConfig"]
    - path: "src/types.ts"
      provides: "Complexity-related type exports"
      contains: "ComplexityResult"
    - path: "src/index.ts"
      provides: "CLI routing for classify-complexity command"
      contains: "classify-complexity"
    - path: "tests/complexity.test.ts"
      provides: "20 labeled test cases plus unit tests for config, override, and rationale"
      min_lines: 150
  key_links:
    - from: "src/index.ts"
      to: "src/lib/complexity.ts"
      via: "import and CLI case"
      pattern: "complexity\\.cmdClassifyComplexity"
    - from: "tests/complexity.test.ts"
      to: "src/lib/complexity.ts"
      via: "import"
      pattern: "from.*complexity"
---

<objective>
Build the complexity classification engine: types, config table, classifier function, CLI command, and validation test suite with 20 labeled cases.

Purpose: This is the core intelligence for Phase 2 -- the classifier that determines how much planning effort a task needs. Everything in Plan 02 depends on these exports.
Output: Working `classify-complexity` CLI command, tested against 20 labeled cases.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complexity-calibration/02-RESEARCH.md
@.planning/phases/02-complexity-calibration/02-VALIDATION.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types.ts (existing types to extend):
```typescript
export type ModelTier = 'opus' | 'sonnet' | 'haiku';
export interface ModelProfile {
  quality: ModelTier;
  balanced: ModelTier;
  budget: ModelTier;
}
```

From src/lib/core.ts (helpers to use):
```typescript
export function output(result: unknown, raw?: boolean, rawValue?: string): void;
export function error(message: string): void;
export function loadConfig(cwd: string): GsdrConfig;
```

From src/index.ts (CLI router pattern -- add new case in switch):
```typescript
// Pattern: case 'command-name': { module.cmdFunction(cwd, args, raw); break; }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complexity module types, config, and classifier function with tests</name>
  <files>src/lib/complexity.ts, src/types.ts, tests/complexity.test.ts</files>
  <behavior>
    - Test: COMPLEXITY_TIERS has entries for 'simple', 'medium', 'complex' with correct research/max_plans/checker/verifier values
    - Test: COMPLEXITY_TIERS.simple.research === 'skip', max_plans === 1, plan_checker === false
    - Test: COMPLEXITY_TIERS.medium.research === 'light', max_plans === 3, plan_checker === true
    - Test: COMPLEXITY_TIERS.complex.research === 'full', max_plans === 10, plan_checker === true
    - Test: getComplexityConfig('simple') returns COMPLEXITY_TIERS.simple
    - Test: getComplexityConfig('invalid') throws or returns null
    - Test: ComplexityResult type has tier, confidence, rationale, signals fields
    - Test: CLASSIFIER_PROMPT contains signal framework sections (file_scope, integration_surface, ambiguity, domain_novelty, cross_cutting)
    - Test: CLASSIFIER_PROMPT contains classification rules (Simple, Medium, Complex)
    - Test: CLASSIFIER_PROMPT contains output format with JSON structure
    - Test: classifyComplexity() with explicit override returns the override tier with rationale mentioning "user override"
    - Test: classifyComplexity() with override='simple' returns { tier: 'simple', confidence: 'high', rationale containing 'override' }
    - Test: 20 labeled test cases (5 Simple, 8 Medium, 5 Complex, 2 edge cases) -- validate the LABELED_TEST_CASES array exists with 20 entries and each has description, projectContext, expectedTier, rationale fields
  </behavior>
  <action>
  Create `src/lib/complexity.ts` with:

  1. **Types** (also export from src/types.ts):
     - `ComplexityResult` interface: `{ tier: 'simple' | 'medium' | 'complex', confidence: 'high' | 'medium' | 'low', rationale: string, signals: { file_scope, integration_surface, ambiguity, domain_novelty, cross_cutting: string[] } }`
     - `ComplexityConfig` interface: `{ tier, research: 'skip' | 'light' | 'full', max_plans: number, plan_checker: boolean, verifier: boolean }`

  2. **COMPLEXITY_TIERS** constant: Record mapping tier names to ComplexityConfig. Values from research doc:
     - simple: research='skip', max_plans=1, plan_checker=false, verifier=false
     - medium: research='light', max_plans=3, plan_checker=true, verifier=true
     - complex: research='full', max_plans=10, plan_checker=true, verifier=true

  3. **CLASSIFIER_PROMPT** constant: The prompt template string from 02-RESEARCH.md (the signal-based classification prompt with {task_description} and {project_summary_from_state_md} placeholders). This is NOT called by the module -- it's exported for skill workflows to embed in their agent prompts.

  4. **getComplexityConfig(tier: string)**: Returns COMPLEXITY_TIERS[tier] or null if invalid.

  5. **classifyComplexity(options: { taskDescription: string, projectContext?: string, override?: string })**:
     - If `override` is provided and valid ('simple'|'medium'|'complex'), return a ComplexityResult with that tier, confidence 'high', rationale "Complexity set to {tier} (user override)", and all signals set to 'low' with empty cross_cutting.
     - If `override` is invalid, throw error.
     - If no override: return the CLASSIFIER_PROMPT filled with taskDescription and projectContext. The actual LLM classification happens in the skill workflow, not here. So this function returns `{ prompt: string }` when no override. Define a union return type: `ComplexityResult | { prompt: string }`.

  6. **LABELED_TEST_CASES** constant: Export the 20 test cases from 02-RESEARCH.md as a typed array.

  7. **cmdClassifyComplexity(cwd, taskDescription, override, raw)**: CLI handler that:
     - If override provided: call classifyComplexity with override, output result
     - If no override: output the filled prompt template (for the skill workflow to use)
     - Uses `output()` and `error()` from core.ts

  Update `src/types.ts` to re-export ComplexityResult and ComplexityConfig types.

  Create `tests/complexity.test.ts` with tests for all behaviors listed above. Tests should import directly from the source module (not via CLI). The labeled test cases test validates the array structure, not actual LLM classification (that's an integration test for later).
  </action>
  <verify>
    <automated>npx vitest run tests/complexity.test.ts</automated>
  </verify>
  <done>All unit tests pass: COMPLEXITY_TIERS config is correct, getComplexityConfig works, override path returns correct ComplexityResult, CLASSIFIER_PROMPT contains all signal framework sections, 20 labeled test cases exist with correct structure</done>
</task>

<task type="auto">
  <name>Task 2: Wire classify-complexity into CLI router and rebuild</name>
  <files>src/index.ts</files>
  <action>
  Add a new case to the CLI router switch in `src/index.ts`:

  ```typescript
  case 'classify-complexity': {
    const overrideIdx = args.indexOf('--override');
    const override = overrideIdx !== -1 ? args[overrideIdx + 1] : undefined;
    const descArgs = args.slice(1).filter(a => !a.startsWith('--') && (overrideIdx === -1 || args.indexOf(a) !== overrideIdx + 1));
    const description = descArgs.join(' ') || undefined;
    if (!description && !override) {
      error('Usage: gsdr-tools classify-complexity "task description" [--override simple|medium|complex]');
    }
    complexity.cmdClassifyComplexity(cwd, description || '', override, raw);
    break;
  }
  ```

  Add import at top: `import * as complexity from './lib/complexity';`

  Update the usage string in the error message to include `classify-complexity`.

  Rebuild: `node esbuild.config.mjs`

  Verify the CLI works end-to-end:
  - `node dist/gsdr-tools.cjs classify-complexity "fix typo in README" --override simple --raw` should output JSON with tier='simple'
  - `node dist/gsdr-tools.cjs classify-complexity "add OAuth2 with RBAC" --raw` should output JSON with the prompt field (since no override, LLM classification deferred)
  </action>
  <verify>
    <automated>node esbuild.config.mjs && node dist/gsdr-tools.cjs classify-complexity "fix typo" --override simple --raw | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); if(d.tier!=='simple') process.exit(1); console.log('OK')"</automated>
  </verify>
  <done>classify-complexity CLI command works: override returns ComplexityResult JSON, no-override returns prompt JSON. Build succeeds. Existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/complexity.test.ts` -- all complexity tests pass
2. `npx vitest run` -- full suite green (no regressions)
3. `node dist/gsdr-tools.cjs classify-complexity "test" --override simple --raw` -- outputs valid JSON
4. `node dist/gsdr-tools.cjs classify-complexity "test" --raw` -- outputs prompt JSON
</verification>

<success_criteria>
- ComplexityResult and ComplexityConfig types exported from src/lib/complexity.ts and src/types.ts
- COMPLEXITY_TIERS constant with correct routing values for all 3 tiers
- CLASSIFIER_PROMPT with full signal framework from research
- classifyComplexity() handles override path (returns ComplexityResult) and auto-detect path (returns prompt)
- 20 labeled test cases defined and structurally validated
- CLI command classify-complexity registered and working
- All tests pass (complexity + existing suite)
</success_criteria>

<output>
After completion, create `.planning/phases/02-complexity-calibration/02-01-SUMMARY.md`
</output>
