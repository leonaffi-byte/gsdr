---
phase: 08-fix-execute-phase-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/execute-phase/SKILL.md
  - execute-plan.md
  - transition.md
  - skills/resume-work/SKILL.md
  - tests/execute-phase-wiring.test.ts
autonomous: true
requirements: [AUTO-03, AUTO-04, AUTO-08, FOUND-03, FOUND-04]
gap_closure: true

must_haves:
  truths:
    - "autonomous generate-report --milestone succeeds at runtime because MILESTONE_NAME is parsed from init JSON"
    - "Executor and planner agents receive execute-plan.md context via @file: reference"
    - "Auto-advance chain propagates --auto flag through phase transitions via transition.md"
  artifacts:
    - path: "skills/execute-phase/SKILL.md"
      provides: "milestone_name in parse list at line 37"
      contains: "milestone_name"
    - path: "execute-plan.md"
      provides: "Supplemental execution context for executor/planner agents"
      min_lines: 150
    - path: "transition.md"
      provides: "Phase transition workflow with auto-advance propagation"
      min_lines: 200
    - path: "tests/execute-phase-wiring.test.ts"
      provides: "Automated verification for all three fixes"
      min_lines: 30
  key_links:
    - from: "skills/execute-phase/SKILL.md"
      to: "autonomous generate-report --milestone"
      via: "MILESTONE_NAME variable parsed from init JSON"
      pattern: "milestone_name"
    - from: "skills/execute-phase/SKILL.md"
      to: "execute-plan.md"
      via: "@${CLAUDE_SKILL_DIR}/../execute-plan.md reference in Task spawn"
      pattern: "execute-plan\\.md"
    - from: "skills/execute-phase/SKILL.md"
      to: "transition.md"
      via: "@${CLAUDE_SKILL_DIR}/../transition.md reference in auto-advance step"
      pattern: "transition\\.md"
    - from: "skills/resume-work/SKILL.md"
      to: "transition.md"
      via: "path reference fixed to plugin root"
      pattern: "transition\\.md"
---

<objective>
Fix three cross-phase wiring gaps in execute-phase identified by v1.0 milestone audit (INT-04, INT-05, INT-06).

Purpose: Without these fixes, end-of-run report generation fails at runtime ($MILESTONE_NAME undefined), executor/planner agents miss execution context (execute-plan.md missing), and auto-advance chain cannot propagate (transition.md missing).

Output: Patched SKILL.md, two ported markdown files at plugin root, fixed resume-work reference, and test suite.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fix-execute-phase-wiring/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test scaffold and SKILL.md milestone_name fix</name>
  <files>tests/execute-phase-wiring.test.ts, skills/execute-phase/SKILL.md</files>
  <action>
**Part A: Create test file** `tests/execute-phase-wiring.test.ts` using vitest. Follow the pattern in `tests/integration-polish.test.ts` (readFileSync + content assertions). Tests to write:

1. **milestone_name parsed (AUTO-08):**
   - `skills/execute-phase/SKILL.md` contains `milestone_name` in the parse list on line 37 (the "Parse JSON for:" line)
   - The parse list line contains both `phase_req_ids` and `milestone_name` (milestone_name added after phase_req_ids)

2. **execute-plan.md exists at plugin root (FOUND-03):**
   - `execute-plan.md` exists at project root (use `fs.existsSync`)
   - File contains `<purpose>` tag (valid workflow structure)
   - File does NOT contain `gsd-tools.cjs` (fully ported, no GSD paths)
   - File does NOT contain `/gsd:` (no GSD slash commands)
   - File references `gsdr-tools.cjs` (GSDR paths used)

3. **execute-plan.md context engineering (FOUND-04):**
   - `agents/gsdr-planner.md` contains `execute-plan.md` reference
   - `skills/execute-phase/SKILL.md` contains `execute-plan.md` reference in Task spawn

4. **transition.md exists at plugin root (AUTO-03):**
   - `transition.md` exists at project root (use `fs.existsSync`)
   - File contains `<purpose>` tag (valid workflow structure)
   - File does NOT contain `gsd-tools.cjs` (fully ported)
   - File does NOT contain `/gsd:` (no GSD slash commands)
   - File does NOT contain `mode="yolo"` and `mode="interactive"` (GSD mode checks replaced)
   - File contains `auto_advance` or `AUTO_CHAIN` or `AUTO_CFG` (GSDR auto-advance pattern)
   - File contains `gsdr-tools.cjs` (GSDR paths used)
   - File contains `/gsdr:` (GSDR slash commands)

5. **transition.md reference in resume-work (AUTO-03):**
   - `skills/resume-work/SKILL.md` contains `${CLAUDE_SKILL_DIR}/../transition.md` (not `./transition.md`)

**Part B: Fix SKILL.md milestone_name parse (INT-04):**

Edit `skills/execute-phase/SKILL.md` line 37. The current "Parse JSON for:" line ends with `phase_req_ids`. Append `, \`milestone_name\`` to the end of that list. The line should end with: `\`phase_req_ids\`, \`milestone_name\`.`

This ensures `$MILESTONE_NAME` is available when line 774 calls `autonomous generate-report --milestone "$MILESTONE_NAME"`.
  </action>
  <verify>
    <automated>npx vitest run tests/execute-phase-wiring.test.ts -t "milestone" --reporter verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with all stubs (most failing -- execute-plan.md and transition.md not yet created). The milestone_name test passes because SKILL.md is already fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Port execute-plan.md to plugin root (reduced version)</name>
  <files>execute-plan.md</files>
  <action>
Port GSD's `~/.claude/get-shit-done/workflows/execute-plan.md` (449 lines) to the GSDR plugin root as a **reduced version** (~200-250 lines). The executor agent (`agents/gsdr-executor.md`) already contains core execution logic (deviation rules, auth gates, checkpoints, TDD, commits, summaries, state updates). This file is supplemental context loaded via `@file:` reference.

**Include these sections (NOT already in gsdr-executor.md):**
1. `<purpose>` -- adapted for GSDR (execute a phase prompt PLAN.md, create SUMMARY.md)
2. `<required_reading>` -- reference `@${CLAUDE_SKILL_DIR}/../references/git-integration.md` (update from GSD path)
3. `init_context` step -- use `${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs` path, extract fields including `milestone_name`
4. `identify_plan` step -- plan identification logic with Pattern A/B/C routing (replace `<if mode="yolo">` with auto-advance check: `AUTO_CHAIN` / `AUTO_CFG` config-get pattern from execute-phase SKILL.md lines 384-386)
5. `record_start_time` step
6. `parse_segments` step -- checkpoint routing to Pattern A/B/C (replace `gsd-executor` with `gsdr-executor`)
7. `init_agent_tracking` step -- agent history tracking
8. `segment_execution` step -- Pattern B segmented execution (replace `gsd-executor` with `gsdr-executor`)
9. `load_prompt` step -- reading PLAN.md, interfaces block handling
10. `previous_phase_check` step -- use `gsdr-tools.cjs phases list --type summaries --raw`
11. `verification_failure_gate` step
12. `record_completion_time` step
13. `update_codebase_map` step (if present in GSD source)
14. `user_setup_generation` step (if present in GSD source)
15. `create_summary` step

**EXCLUDE these sections (already in gsdr-executor.md):**
- `<authentication_gates>` (executor agent lines 193-206)
- `<deviation_rules>` and `<deviation_documentation>` (executor agent lines 110-181)
- `<tdd_plan_execution>` (executor agent lines 297-309)
- `<task_commit>` protocol (executor agent lines 311-342)
- `checkpoint_protocol` and `checkpoint_return_for_orchestrator` (executor agent lines 219-285)

**Required path replacements throughout:**
- `$HOME/.claude/get-shit-done/bin/gsd-tools.cjs` -> `${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs`
- `gsd-executor` -> `gsdr-executor`
- `gsd-planner` -> `gsdr-planner`
- `/gsd:` -> `/gsdr:`
- `gsd-tools` -> `gsdr-tools`

**Required mode replacements:**
- `<if mode="yolo">` blocks -> auto-advance detection: check `AUTO_CHAIN` and `AUTO_CFG` via `gsdr-tools.cjs config-get`
- `<if mode="interactive">` blocks -> non-auto path

After creating the file, verify: `grep -c "gsd-tools\|/gsd:" execute-plan.md` returns 0 (no GSD references remain).
  </action>
  <verify>
    <automated>npx vitest run tests/execute-phase-wiring.test.ts -t "execute-plan" --reporter verbose 2>&1 | tail -20</automated>
  </verify>
  <done>execute-plan.md exists at plugin root with ~200-250 lines of supplemental execution context. Zero GSD path references. All execute-plan tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Port transition.md to plugin root and fix resume-work reference</name>
  <files>transition.md, skills/resume-work/SKILL.md</files>
  <action>
Port GSD's `~/.claude/get-shit-done/workflows/transition.md` (544 lines) to the GSDR plugin root. This is a full port (not reduced) because transition logic is NOT duplicated in any GSDR agent.

**Port all steps from GSD transition.md:**
1. `<required_reading>` -- keep file list (STATE.md, PROJECT.md, ROADMAP.md, plan/summary files)
2. `<purpose>` -- mark current phase complete and advance to next
3. `load_project_state` step -- replace `gsd-tools.cjs` path with `${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs`
4. `verify_completion` step -- replace `<if mode="yolo">` with auto-advance detection pattern:
   ```bash
   AUTO_CHAIN=$(node "${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs" config-get workflow._auto_chain_active 2>/dev/null || echo "false")
   AUTO_CFG=$(node "${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs" config-get workflow.auto_advance 2>/dev/null || echo "false")
   ```
   If either is `"true"` -> auto-approve path. Otherwise -> interactive path.
5. `cleanup_handoff` step -- no changes needed beyond path replacement
6. `update_roadmap_and_state` step -- replace `gsd-tools.cjs` path
7. `archive_prompts` step -- keep as-is
8. `evolve_project` step -- keep as-is (PROJECT.md evolution)
9. `update_current_position_after_transition` step -- replace `gsd-tools.cjs` path
10. `update_project_reference` step -- keep as-is
11. `review_accumulated_context` step -- keep as-is
12. `update_session_continuity_after_transition` step -- keep as-is
13. `offer_next_phase` step -- **CRITICAL ADAPTATION:**
    - Replace `<if mode="yolo">` with auto-advance check (AUTO_CHAIN/AUTO_CFG)
    - Replace `SlashCommand("/gsd:plan-phase [X+1] --auto")` with: "Invoke `/gsdr:plan-phase ${NEXT_PHASE} --auto`" (guidance for orchestrator, not SlashCommand call -- see Pitfall 3 in research)
    - Replace `SlashCommand("/gsd:discuss-phase [X+1] --auto")` with: "Invoke `/gsdr:discuss-phase ${NEXT_PHASE} --auto`"
    - Replace `SlashCommand("/gsd:complete-milestone {version}")` with: "Invoke `/gsdr:complete-milestone ${VERSION}`"
    - Route B (milestone complete): Replace `gsd-tools.cjs config-set` with `gsdr-tools.cjs config-set`
    - All `/gsd:` in display text -> `/gsdr:`
14. `<partial_completion>` section -- keep as-is
15. `<success_criteria>` section -- keep as-is
16. `<implicit_tracking>` section -- keep as-is

**Required path replacements throughout:**
- `$HOME/.claude/get-shit-done/bin/gsd-tools.cjs` -> `${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs`
- `/gsd:` -> `/gsdr:`
- All `<if mode="yolo">` -> auto-advance check pattern
- All `<if mode="interactive" OR="custom with gates.confirm_transition true">` -> non-auto path
- Remove all `</if>` closing tags that correspond to removed `<if mode=...>` tags

**After creating transition.md, fix resume-work reference:**

Edit `skills/resume-work/SKILL.md` line 253. Change:
```
- **Transition** → ./transition.md
```
to:
```
- **Transition** → ${CLAUDE_SKILL_DIR}/../transition.md
```

The relative path `./transition.md` resolves to `skills/resume-work/transition.md` which is wrong. It should resolve to the plugin root.

**Final verification:** Run `grep -c "gsd-tools\|/gsd:\|mode=\"yolo\"\|mode=\"interactive\"" transition.md` -- must return 0.
  </action>
  <verify>
    <automated>npx vitest run tests/execute-phase-wiring.test.ts --reporter verbose 2>&1 | tail -30</automated>
  </verify>
  <done>transition.md exists at plugin root with full transition workflow. Zero GSD references, zero mode="yolo"/mode="interactive" checks. resume-work SKILL.md references ${CLAUDE_SKILL_DIR}/../transition.md. All tests pass.</done>
</task>

</tasks>

<verification>
All three INT gaps closed:
1. INT-04: `grep "milestone_name" skills/execute-phase/SKILL.md` finds it in parse list
2. INT-05: `test -f execute-plan.md && echo EXISTS` confirms file at plugin root
3. INT-06: `test -f transition.md && echo EXISTS` confirms file at plugin root

Full test suite: `npx vitest run --reporter verbose` (all existing + new tests pass)
</verification>

<success_criteria>
- `npx vitest run tests/execute-phase-wiring.test.ts --reporter verbose` -- all tests pass
- `npx vitest run --reporter verbose` -- full suite green (no regressions)
- `grep "milestone_name" skills/execute-phase/SKILL.md` -- found in parse list
- `grep -c "gsd-tools\|/gsd:" execute-plan.md` -- returns 0
- `grep -c "gsd-tools\|/gsd:\|mode=\"yolo\"\|mode=\"interactive\"" transition.md` -- returns 0
- `grep "CLAUDE_SKILL_DIR.*transition.md" skills/resume-work/SKILL.md` -- found
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-execute-phase-wiring/08-01-SUMMARY.md`
</output>
