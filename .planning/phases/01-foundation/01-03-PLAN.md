---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/new-project/SKILL.md
  - skills/plan-phase/SKILL.md
  - skills/execute-phase/SKILL.md
  - skills/research-phase/SKILL.md
  - skills/discuss-phase/SKILL.md
  - skills/verify-work/SKILL.md
  - skills/resume-work/SKILL.md
  - skills/quick/SKILL.md
  - skills/progress/SKILL.md
  - skills/debug/SKILL.md
  - skills/map-codebase/SKILL.md
  - skills/settings/SKILL.md
  - skills/health/SKILL.md
  - skills/help/SKILL.md
  - skills/add-phase/SKILL.md
  - skills/insert-phase/SKILL.md
  - skills/remove-phase/SKILL.md
  - skills/validate-phase/SKILL.md
  - skills/complete-milestone/SKILL.md
  - skills/new-milestone/SKILL.md
  - skills/add-todo/SKILL.md
  - skills/check-todos/SKILL.md
  - skills/add-tests/SKILL.md
  - skills/pause-work/SKILL.md
  - skills/set-profile/SKILL.md
  - skills/cleanup/SKILL.md
  - skills/update/SKILL.md
  - skills/plan-milestone-gaps/SKILL.md
  - skills/audit-milestone/SKILL.md
  - skills/list-phase-assumptions/SKILL.md
  - skills/reapply-patches/SKILL.md
  - agents/gsdr-executor.md
  - agents/gsdr-planner.md
  - agents/gsdr-phase-researcher.md
  - agents/gsdr-project-researcher.md
  - agents/gsdr-research-synthesizer.md
  - agents/gsdr-debugger.md
  - agents/gsdr-codebase-mapper.md
  - agents/gsdr-verifier.md
  - agents/gsdr-plan-checker.md
  - agents/gsdr-integration-checker.md
  - agents/gsdr-nyquist-auditor.md
  - agents/gsdr-roadmapper.md
  - templates/
  - references/
  - hooks/hooks.json
autonomous: true
requirements: [FOUND-03, FOUND-04, FOUND-05]

must_haves:
  truths:
    - "All GSD commands exist as /gsdr:* skills in skills/ directory"
    - "All GSD agents exist as gsdr-* agents in agents/ directory"
    - "Skills reference ${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs for CLI tool access"
    - "Sub-agent spawns use path-only references (no file content inlined)"
    - "XML-structured plan format is preserved in agent workflow prompts"
    - "Templates and references are available for agent consumption"
  artifacts:
    - path: "skills/plan-phase/SKILL.md"
      provides: "Plan-phase orchestrator skill"
      contains: "gsdr:plan-phase"
    - path: "skills/execute-phase/SKILL.md"
      provides: "Execute-phase orchestrator skill"
      contains: "gsdr:execute-phase"
    - path: "agents/gsdr-executor.md"
      provides: "Plan execution agent"
      contains: "gsdr-executor"
    - path: "agents/gsdr-planner.md"
      provides: "Phase planning agent"
      contains: "gsdr-planner"
    - path: "templates/state.md"
      provides: "STATE.md template"
      contains: "gsd_state_version"
    - path: "hooks/hooks.json"
      provides: "Lifecycle hooks configuration"
  key_links:
    - from: "skills/plan-phase/SKILL.md"
      to: "agents/gsdr-planner.md"
      via: "agent: gsdr-planner in frontmatter"
      pattern: "agent:.*gsdr-planner"
    - from: "skills/execute-phase/SKILL.md"
      to: "agents/gsdr-executor.md"
      via: "agent: gsdr-executor in frontmatter"
      pattern: "agent:.*gsdr-executor"
    - from: "agents/gsdr-executor.md"
      to: "dist/gsdr-tools.cjs"
      via: "CLAUDE_SKILL_DIR path reference"
      pattern: "CLAUDE_SKILL_DIR.*gsdr-tools"
    - from: "agents/gsdr-planner.md"
      to: "templates/"
      via: "path references to template files"
      pattern: "templates/"
---

<objective>
Port all GSD commands to GSDR plugin skills, port all GSD agents to GSDR agents, and copy markdown assets (templates, references, hooks).

Purpose: Create the complete Claude Code plugin content layer -- the skills users invoke and the agents that do the work. This is independent of the TypeScript tooling (which is built in Plans 01-02).
Output: Full skills/, agents/, templates/, references/, and hooks/ directories with all content ported from GSD to GSDR naming.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port all GSD commands to GSDR plugin skills</name>
  <files>skills/*/SKILL.md (all 31 skill directories)</files>
  <action>
Port all 32 GSD commands from ~/.claude/commands/gsd/*.md to GSDR plugin skills in skills/*/SKILL.md format.

For EACH command file in ~/.claude/commands/gsd/:
1. Read the source command file
2. Create a directory under skills/ named after the command (e.g., skills/plan-phase/)
3. Create SKILL.md inside that directory

**Frontmatter changes for ALL skills:**
- Change `name:` from `gsd:command-name` to `gsdr:command-name`
- Keep `description:`, `argument-hint:`, `allowed-tools:` as-is
- If the skill has `agent:`, rename from `gsd-agentname` to `gsdr-agentname`
- Add `context: fork` for skills that delegate to agents (they need fresh context per FOUND-04)
- Add `disable-model-invocation: true` for workflow skills (to stay within description budget per Pitfall 6)

**Content changes for ALL skills:**
- Replace all path references from `$HOME/.claude/get-shit-done/` or `C:/Users/leofu/.claude/get-shit-done/` to use `${CLAUDE_SKILL_DIR}/..` relative paths
- Replace `gsd-tools.cjs` references with `${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs`
- Replace `@C:/Users/leofu/.claude/get-shit-done/workflows/` with `${CLAUDE_SKILL_DIR}/../` or inline the workflow content
- Replace `@C:/Users/leofu/.claude/get-shit-done/templates/` with `${CLAUDE_SKILL_DIR}/../templates/`
- Replace `@C:/Users/leofu/.claude/get-shit-done/references/` with `${CLAUDE_SKILL_DIR}/../references/`
- Replace all `gsd-` agent references with `gsdr-` (e.g., `gsd-planner` -> `gsdr-planner`)
- Replace `/gsd:` command references with `/gsdr:` in help text and cross-references
- Remove any multi-runtime references (OpenCode, Gemini, Codex, Copilot)

**Skills that DON'T need agent delegation** (simple/utility skills -- no context: fork):
join-discord, help, settings, set-profile, update, reapply-patches

**Skills that DO need agent delegation** (workflow skills -- context: fork + agent):
plan-phase, execute-phase, research-phase, discuss-phase, verify-work, new-project, quick, debug, map-codebase, resume-work, new-milestone, complete-milestone, audit-milestone, plan-milestone-gaps

Note: The `join-discord` command may not apply to GSDR. Skip it or create a placeholder.
  </action>
  <verify>
    <automated>cd C:/projects/gsdr && skill_count=$(find skills -name "SKILL.md" | wc -l) && echo "Skills created: $skill_count" && grep -rl "gsd:" skills/ --include="*.md" | grep -v "gsdr:" | head -5 && echo "---" && grep -rl "gsd-tools.cjs" skills/ --include="*.md" | head -3 && echo "Path refs check done"</automated>
  </verify>
  <done>At least 30 skills exist in skills/*/SKILL.md format. All skills use gsdr: namespace (no bare gsd: references). All path references use ${CLAUDE_SKILL_DIR} (no hardcoded ~/.claude/ paths). Skills with agent delegation have context: fork. No multi-runtime references.</done>
</task>

<task type="auto">
  <name>Task 2: Port all GSD agents and copy markdown assets</name>
  <files>agents/gsdr-*.md, templates/*, references/*, hooks/hooks.json</files>
  <action>
Port all 12 GSD agents and copy supporting markdown assets.

**Agents** (port from ~/.claude/agents/gsd-*.md to agents/gsdr-*.md):
For EACH agent file:
1. Read the source from ~/.claude/agents/gsd-{name}.md
2. Create agents/gsdr-{name}.md

**Agent frontmatter changes:**
- Rename `name:` from `gsd-agentname` to `gsdr-agentname`
- Keep `description:`, `tools:`, `color:` as-is
- If `skills:` field references gsd skills, rename to gsdr equivalents

**Agent content changes:**
- Replace ALL path references from `$HOME/.claude/get-shit-done/` to use relative paths based on the plugin directory structure
- Replace `gsd-tools.cjs` with `gsdr-tools.cjs` and update path to `${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs` (note: agents don't have CLAUDE_SKILL_DIR, so use the pattern from the skill that spawns them -- the skill passes the path)
- Actually, per the research: agents reference paths relative to .planning/ (project working directory). For CLI tool references, use a variable pattern like: `node "${GSDR_TOOLS}"` where GSDR_TOOLS is set by the spawning skill. Or keep the `$HOME/.claude/` pattern but document it.
- BETTER approach per research: In the skill that spawns the agent, set GSDR_TOOLS path in the execution_context. The agent reads it from there. Example: `GSDR_TOOLS="${CLAUDE_SKILL_DIR}/../dist/gsdr-tools.cjs"` in the skill, then agent uses `node "$GSDR_TOOLS"`. Since skills CAN compute the path but agents cannot, the skill must provide it.
- Replace all `gsd-` references with `gsdr-` in agent names, command references, file references
- Replace `/gsd:` command references with `/gsdr:`
- Remove multi-runtime code paths
- Preserve the path-only context engineering pattern (files_to_read blocks should use @path references, NOT inline content) -- this is FOUND-04
- Preserve XML plan format in execution workflows (task/checkpoint/verify tags) -- this is FOUND-05

**Templates** (copy from ~/.claude/get-shit-done/templates/ to templates/):
- Copy ALL .md template files
- Copy the codebase/ subdirectory if it exists
- Copy config.json template
- In each template, replace "GSD" with "GSDR" and "gsd" with "gsdr" in user-facing text
- Keep template variable placeholders ({phase}, {plan}, etc.) unchanged

**References** (copy from ~/.claude/get-shit-done/references/ to references/):
- Copy ALL .md reference files
- Replace "GSD"/"gsd" references with "GSDR"/"gsdr" where they refer to the product/commands
- Keep technical content unchanged (git patterns, verification patterns, etc.)

**Hooks** (create hooks/hooks.json):
- Create a minimal hooks.json: `{ "hooks": [] }`
- GSD's hooks are registered in ~/.claude/settings.json globally. For the plugin, hooks go in hooks/hooks.json. Port any relevant hooks (e.g., post-tool linting) but start with empty hooks -- they can be added later.
  </action>
  <verify>
    <automated>cd C:/projects/gsdr && agent_count=$(find agents -name "gsdr-*.md" | wc -l) && template_count=$(find templates -name "*.md" | wc -l) && ref_count=$(find references -name "*.md" | wc -l) && echo "Agents: $agent_count, Templates: $template_count, References: $ref_count" && test -f hooks/hooks.json && echo "hooks.json exists" && grep -rl "gsd-executor" agents/ | head -3; echo "Agent rename check done"</automated>
  </verify>
  <done>12 agents exist as agents/gsdr-*.md. All agents use gsdr-* naming. Templates and references directories populated with ported markdown files. hooks/hooks.json exists. Agent definitions preserve path-only context engineering (files_to_read uses @path references). XML plan format preserved in executor agent workflow.</done>
</task>

</tasks>

<verification>
1. `find skills -name "SKILL.md" | wc -l` shows >= 30
2. `find agents -name "gsdr-*.md" | wc -l` shows 12
3. `grep -rl "gsd:" skills/ | grep -v "gsdr:"` returns empty (all namespaced)
4. `grep -l "files_to_read" agents/gsdr-executor.md` confirms path-based context engineering
5. `grep -l "<task" agents/gsdr-executor.md` confirms XML plan format preserved
6. `test -f hooks/hooks.json` passes
7. `find templates -name "*.md" | wc -l` shows >= 15
8. `find references -name "*.md" | wc -l` shows >= 10
</verification>

<success_criteria>
- All GSD commands ported to GSDR plugin skills with /gsdr: namespace
- All GSD agents ported with gsdr-* naming
- Skills use ${CLAUDE_SKILL_DIR} for portable path resolution
- Sub-agent spawns use path-only references (FOUND-04)
- XML plan format preserved in agent workflows (FOUND-05)
- Templates and references available for agent use
- No hardcoded ~/.claude/ paths remain
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
