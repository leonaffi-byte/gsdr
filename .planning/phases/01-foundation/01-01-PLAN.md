---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - esbuild.config.mjs
  - .claude-plugin/plugin.json
  - src/types.ts
  - src/lib/core.ts
  - src/lib/frontmatter.ts
  - src/lib/config.ts
  - .gitignore
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]

must_haves:
  truths:
    - "Project compiles TypeScript without errors"
    - "esbuild config exists and targets CJS output"
    - "Plugin manifest declares gsdr namespace"
    - "Multi-runtime code is stripped (no OpenCode/Gemini/Codex/Copilot references)"
    - "Core utilities (output, error, toPosixPath, frontmatter parsing) are available as TS modules"
  artifacts:
    - path: "package.json"
      provides: "npm package definition with build scripts"
      contains: "gsdr"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
    - path: "esbuild.config.mjs"
      provides: "Build pipeline configuration"
      contains: "gsdr-tools.cjs"
    - path: ".claude-plugin/plugin.json"
      provides: "Claude Code plugin manifest"
      contains: "gsdr"
    - path: "src/types.ts"
      provides: "Shared type definitions"
      exports: ["GsdrConfig", "StateFrontmatter", "PhaseInfo", "InitResult"]
    - path: "src/lib/core.ts"
      provides: "Core utilities (output, error, paths, model profiles)"
      exports: ["output", "error", "toPosixPath", "loadConfig", "MODEL_PROFILES"]
    - path: "src/lib/frontmatter.ts"
      provides: "YAML frontmatter extraction and reconstruction"
      exports: ["extractFrontmatter", "reconstructFrontmatter"]
    - path: "src/lib/config.ts"
      provides: "Config loading and management"
      exports: ["cmdConfigEnsureSection", "cmdConfigSet", "cmdConfigGet"]
  key_links:
    - from: "src/lib/config.ts"
      to: "src/lib/core.ts"
      via: "import { loadConfig, output, error }"
      pattern: "import.*from.*core"
    - from: "src/lib/frontmatter.ts"
      to: "src/types.ts"
      via: "type imports"
      pattern: "import.*from.*types"
---

<objective>
Scaffold the GSDR project structure and port the foundational TypeScript modules that all other modules depend on.

Purpose: Establish the build pipeline, type system, and core utilities so that subsequent plans can port remaining modules against a working foundation.
Output: Compilable TypeScript project with core.ts, frontmatter.ts, config.ts, and types.ts ready for downstream consumption.
</objective>

<execution_context>
@C:/Users/leofu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leofu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project structure and type definitions</name>
  <files>package.json, tsconfig.json, esbuild.config.mjs, .claude-plugin/plugin.json, src/types.ts, .gitignore</files>
  <action>
Create the GSDR project scaffold:

1. **package.json**: Create with name "gsdr", version "1.0.0", bin entry pointing to "bin/install.js", scripts for "build" (node esbuild.config.mjs), "test" (vitest run), "prepublishOnly" (npm run build). files array: ["bin", ".claude-plugin", "skills", "agents", "hooks", "templates", "references", "dist"]. devDependencies: typescript 5.7+, esbuild 0.24+, @types/node 18+, vitest 3.x.

2. **tsconfig.json**: target ES2022, module ESNext, moduleResolution bundler, strict true, esModuleInterop true, skipLibCheck true, outDir dist, rootDir src, declaration true, declarationDir dist/types, sourceMap true, types ["node"]. Include src/**, exclude node_modules/dist/tests.

3. **esbuild.config.mjs**: Import esbuild, build with entryPoints ['src/index.ts'], bundle true, platform 'node', target 'node18', format 'cjs', outfile 'dist/gsdr-tools.cjs', external [] (bundle everything), minify false, sourcemap true.

4. **.claude-plugin/plugin.json**: name "gsdr", description "GSD Reloaded - Autonomous spec-driven development for Claude Code", version "1.0.0".

5. **src/types.ts**: Port type definitions from research. Include GsdrConfig, StateFrontmatter, PhaseInfo, InitResult, and any other interfaces needed. Rename all "gsd" prefixes to "gsdr" in type names where appropriate. Export all types.

6. **.gitignore**: node_modules/, dist/, *.tsbuildinfo, .env.

7. Run `npm install` to create node_modules and package-lock.json.
  </action>
  <verify>
    <automated>cd C:/projects/gsdr && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>package.json exists with correct fields, tsconfig.json compiles without errors, esbuild.config.mjs exists, plugin.json has gsdr namespace, types.ts exports all shared interfaces, .gitignore excludes build artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Port core.ts, frontmatter.ts, and config.ts from GSD CJS to TypeScript</name>
  <files>src/lib/core.ts, src/lib/frontmatter.ts, src/lib/config.ts</files>
  <action>
Port three foundational GSD CJS modules to TypeScript. These modules are dependencies for ALL other modules, so they must be ported first.

**src/lib/core.ts** (port from ~/.claude/get-shit-done/bin/lib/core.cjs, ~492 lines):
- Port toPosixPath, escapeRegex, output (with @file: tmpfile pattern for large payloads), error (process.exit)
- Port MODEL_PROFILES table but rename all "gsd-*" agent names to "gsdr-*" (e.g., "gsd-planner" becomes "gsdr-planner"). This is the FOUND-01 multi-runtime strip -- only keep Claude Code model names
- Port loadConfig (reads .planning/config.json with defaults), getMilestoneInfo, getMilestonePhaseFilter
- Port any other exported helpers (parseFrontmatterValue, readFileIfExists, etc.)
- Strip any multi-runtime conditional logic (OpenCode, Gemini, Codex, Copilot paths)
- Add proper TypeScript types for all function parameters and return values
- Import types from ../types.ts where appropriate

**src/lib/frontmatter.ts** (port from ~/.claude/get-shit-done/bin/lib/frontmatter.cjs, ~299 lines):
- Port extractFrontmatter (parses YAML between --- delimiters, returns {frontmatter, body})
- Port reconstructFrontmatter (rebuilds file from frontmatter object + body string)
- Port cmdFrontmatterGet, cmdFrontmatterSet, cmdFrontmatterMerge, cmdFrontmatterValidate
- Add TypeScript types. The frontmatter object should be typed as Record<string, unknown> with specific overloads for known schemas

**src/lib/config.ts** (port from ~/.claude/get-shit-done/bin/lib/config.cjs, ~169 lines):
- Port cmdConfigEnsureSection, cmdConfigSet, cmdConfigGet
- Use typed GsdrConfig from types.ts
- Import helpers from core.ts

For ALL modules:
- Convert require() to import statements
- Convert module.exports to named exports
- Add explicit return types to all functions
- Replace var/let with const where appropriate
- Use path.join for file paths, toPosixPath for display paths
  </action>
  <verify>
    <automated>cd C:/projects/gsdr && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>All three modules compile without TypeScript errors. core.ts exports output, error, toPosixPath, loadConfig, MODEL_PROFILES with gsdr-* agent names. frontmatter.ts exports extractFrontmatter, reconstructFrontmatter. config.ts exports config commands. No require() calls remain. No multi-runtime references (OpenCode, Gemini, Codex, Copilot) in any file.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "OpenCode\|Gemini\|Codex\|Copilot" src/` returns no results
3. `grep -r "require(" src/` returns no results (all imports are ESM-style)
4. `.claude-plugin/plugin.json` contains `"name": "gsdr"`
5. `src/types.ts` exports GsdrConfig, StateFrontmatter, PhaseInfo, InitResult
</verification>

<success_criteria>
- TypeScript project compiles cleanly
- Core modules (core.ts, frontmatter.ts, config.ts) are fully ported with types
- No multi-runtime code remains
- Plugin manifest declares gsdr namespace
- Build pipeline (esbuild) is configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
